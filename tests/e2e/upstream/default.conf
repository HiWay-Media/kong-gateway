# The thing being protected. It answers anything with 200 and echoes the path it was asked for, so a
# test can tell "Kong forwarded the request" from "Kong answered on its own".
server {
    listen 80;
    default_type application/json;

    location / {
        return 200 '{"upstream":"reached","path":"$request_uri"}';
    }
}
